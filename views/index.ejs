
    <h1>Test</h1>
    <h2>Hello <%= nama %> </h2>

    <h4>List</h4>
    <% if (mahasiswa.length < 1) { %>
     <p>List Empty</p>
    <% } else{ %>

    <% mahasiswa.forEach(mhs => { %>
        <ul>
            <li>Nama: <%= mhs.nama %>  </li>
            <li>Email: <%= mhs.email  %> </li>
        </ul>
     
    <% }) %>

    <% } %> 
